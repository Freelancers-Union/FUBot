services:
  fubot:
    build: .
    image: fu/bot/${ENVIRONMENT}
    container_name: fubot-${ENVIRONMENT}
    ports:
    - "80:80"
    restart: unless-stopped
    environment:
    - LOGLEVEL=${LOGLEVEL}
    - DISCORDTOKEN=${DISCORDTOKEN}
    - CENSUS_TOKEN=${CENSUS_TOKEN}
