name: Docker Build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: | 
      echo "hello world"
      docker build . --file Dockerfile --tag fubot:$(date +%s)
