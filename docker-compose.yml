version: "3"

services:
  fubot:
    ports:
    - "80:80"
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
    - DISCORDTOKEN=${DISCORDTOKEN}
    - CENSUS_TOKEN=${CENSUS_TOKEN}
